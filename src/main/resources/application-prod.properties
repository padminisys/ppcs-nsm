# Production Kubernetes Client Configuration
# Use service account token mounted in the pod
%prod.quarkus.kubernetes-client.kubeconfig-path=
%prod.quarkus.kubernetes-client.trust-certs=true

# Production Logging Configuration
%prod.quarkus.log.level=INFO
%prod.quarkus.log.category."org.padminisys".level=INFO